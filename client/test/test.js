var assert = chai.assert;

describe("my function", function() {
  it("is a function", function() {
    assert.equal(true, true);
  });
});

describe("Does a string, string?", function() {
  it("IT DOES STRING!", function() {
    assert.equal("wat", "wat");
  });
});
