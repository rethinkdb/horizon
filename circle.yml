## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezone

  # Version of ruby to use
  node:
    version:
      4.2.2

## Customize dependencies
dependencies:
  pre:
    - cd client; npm install; ./node_modules/gulp-cli/bin/gulp compile-tests
    - cd server; npm install

## Customize test commands
test:
  post:
    - cd server; npm test
    - cd client; mocha-phantomjs test/test-es5.html
